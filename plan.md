# Config

- create a config.template folder

# Sorting of Yaml Config Files

- accounts.yaml sort by open, then name
- categories sort by name
- config.yaml sort by node name
- tags.yaml sort by name
- payee_rules/*.yaml sort by name


# Import
- make sure after a transaction is imported, it is then processed and tagged.  Then the rules are run on it, only if there is currently no category assigned.

# Git History

Delete all git history, and reset.


# Agent

Update Gemini.md